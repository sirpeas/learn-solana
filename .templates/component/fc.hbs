import { FC } from 'react';

import { Props } from './types';

const {{ firstUpperCase name }}: FC<Props> = (props) => {
  const { } = props;

  return (
    <div>

    </div>
  );
};
{{ firstUpperCase name }}.displayName = '{{ firstUpperCase name }}';

export default {{ firstUpperCase name }};
